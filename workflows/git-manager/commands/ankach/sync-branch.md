---
description: "Sync the current branch with its correct long-lived base (main/master or dev/stage), by rebase or merge."
---

Run the `sync-branch` skill to sync the current branch with its base. Read and follow `.claude/skills/sync-branch/SKILL.md` step by step.

$ARGUMENTS
